import React from 'react'

const UserSocial = ({id, mode}) => {
    return (
        <div>
            <h2>The {mode} for user {id}</h2>
            
        </div>
    )
}

export default UserSocial
